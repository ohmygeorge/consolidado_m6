<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
    <div class="container-fluid">

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{#if home}}active{{/if}}" aria-current="page" href="/home">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if new}}active{{/if}}" href="/animes/new">Agregar Anime</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link " href="/animes">API</a>
                </li>
            </ul>
            {{!-- BUSCADOR DE ANIME POR NOMBRE??? --}}
            <form class="d-flex" role="search" id="buscarNombre">
                <input class="form-control me-2" type="search" placeholder="Nombre Anime" aria-label="Search" id="nombreAnime">
                <button class="btn btn-outline-success" type="submit">Buscar</button>
            </form>
        </div>
    </div>
</nav>
<script>
    let formBuscar = document.getElementById('buscarNombre');
    formBuscar.addEventListener('submit', async (event) => {
        event.preventDefault();
        try{
        let nombre = nombreAnime.value;
        document.location.href="/anime/"+nombre;
        }
        catch (error) {
            console.log(error)
        }
    })
</script>